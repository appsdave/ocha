# T-001 Coordinator Brief

**Task:** Line one Line two Line three
**Session:** S-001-01 · **Branch:** `agent`

---

## Execution Brief

### 1. Goal (refined)

This is a **smoke-test task** used to validate the ocha end-to-end workflow pipeline — from operator prompt ingestion, through coordinator refinement, to downstream lead/builder/reviewer handoff. The operator payload ("Line one / Line two / Line three") carries no domain-specific instructions; the success criterion is that the coordinator produces a well-formed brief, the lead acknowledges it, the builder finds nothing to change, and the reviewer confirms a clean pass. No production code changes are expected.

### 2. Relevant files

| File | Role | Action |
|---|---|---|
| `app/orchestrator.py` | All | Read — understand launch flow, role directories, prompt assembly |
| `app/roles/coordinator.md` | Coordinator | Read — role contract (this session) |
| `app/roles/lead.md` | Lead | Read — downstream expectations |
| `app/roles/builder.md` | Builder | Read — downstream expectations |
| `app/roles/reviewer.md` | Reviewer | Read — downstream expectations |
| `app/state.py` | All | Read — data model for tasks/workers |
| `docs/` | Coordinator | Write — deposit this brief |
| `.ocha/tasks/T-001/prompt.md` | All | Read — persisted operator prompt |

### 3. Recommended changes

**Phase 0 — Coordinator (this session)**
1. Produce this execution brief in `docs/`.
2. No code changes required.

**Phase 1 — Lead**
1. Read this brief; confirm the task is a no-op smoke test.
2. Emit a short acknowledgement plan in `planning/`.

**Phase 2 — Builder**
1. Read the lead plan; confirm no source edits are needed.
2. Skip implementation; report clean status.

**Phase 3 — Reviewer**
1. Verify no files were changed outside owned directories.
2. Confirm the pipeline executed without errors; mark task complete.

### 4. Safe partitioning

| Directory | Owning Role | Notes |
|---|---|---|
| `docs/` | Coordinator | Briefs and audit trail |
| `planning/` | Lead | Execution plans |
| `app/` | Builder / Reviewer | Source code (no edits expected for this task) |
| `.ocha/` | Orchestrator (system) | Read-only for all roles |

All roles operate on the shared **`agent`** branch. No additional branches required.

---

*Generated by coordinator session S-001-01 for task T-001.*
